# Local development
# Vite dev server proxies /api → localhost:3001 (see vite.config.js)
# Use the full URL here so axios bypasses the Vite proxy and hits backend directly.
VITE_API_URL=http://localhost:3001
